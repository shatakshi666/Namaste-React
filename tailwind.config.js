/** @type {import('tailwindcss').Config} */
module.exports = {
  content: [
    "./src/**/*.{js,jsx,ts,tsx}",
    // This defined line in content indicates that it takes a list of all files 
    // that we are using with the tailwind
  ],
  theme: {
    extend: {},
  },
  plugins: [],
}

